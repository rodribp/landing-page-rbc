---
const { href, class: className } = Astro.props;
---

<a
  href={href}
  class=`text-white font-semibold p-2 px-6 hover:bg-black duration-200 bg-black/60 text-lg inline-block ${className}`
  ><slot /></a
>
